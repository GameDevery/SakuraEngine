using SB;
using SB.Core;

[TargetScript]
public static class SkrImageCoder
{
    static SkrImageCoder()
    {
        /*
        var ImageCoder = Engine
            .Module("SkrImageCoder")
            .Depend(Visibility.Public, "SkrRT")
            .IncludeDirs(Visibility.Public, Path.Combine(SourceLocation.Directory(), "include"))
        */
    }
}